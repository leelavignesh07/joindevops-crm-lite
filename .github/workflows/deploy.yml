name: Deploy to EC2

# Deploys the app to the single EC2 instance described in DEPLOYMENT.md
# whenever main is updated. Requires these repo secrets (see DEPLOYMENT.md):
#   EC2_HOST        - public IP or DNS of the instance
#   EC2_USER        - ssh user (e.g. ubuntu)
#   EC2_SSH_KEY     - private key matching the instance's key pair
#   EC2_APP_DIR     - absolute path of the git checkout on the instance,
#                     e.g. /home/ubuntu/joindevops-crm-lite
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd "${{ secrets.EC2_APP_DIR }}"
            git fetch origin main
            git reset --hard origin/main
            docker compose -f docker-compose.prod.yml --env-file .env up -d --build
            docker image prune -f
